server {
  listen 80;
  server_name _;
  location /api/ {
    proxy_pass http://127.0.0.1:4000/api/;
    proxy_set_header Host $host;
  }
  location / {
    root /var/www/ecs;
    try_files $uri /index.html;
  }
}
